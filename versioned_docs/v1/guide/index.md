---
title: Getting started
description: The v1 getting-started page, showing that each version has its own sidebar.
sidebar_position: 1
---

# Getting started (v1)

Each version keeps its own pages, its own sections and its own sidebar. Search
is scoped too: a query typed while reading v1 returns v1 pages only.

Links between pages stay inside the version, so nothing here quietly sends you
to the current release.

- [Configuration in v1](./configuration.md)
