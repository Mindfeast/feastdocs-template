node_modules
dist
.angular

# Generated by the content build; the image rebuilds them.
src/app/generated
public/docs-assets
public/search-index.json

# NOTE: .git is deliberately NOT ignored. The build reads git history to show
# "last updated by" on every page. Add `.git` here only if you do not want
# author attribution — pages then fall back to the file date.
